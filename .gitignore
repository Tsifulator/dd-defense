# --- Secrets: NEVER commit ---
.env
.env.*
!.env.example
*.key
*.pem

# --- macOS ---
.DS_Store

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# --- Generated audit outputs ---
out/
out_*/
.preview_url
*_webapp_sample_result.html

# --- Case database (real client data — never commit) ---
data/
*.db
*.sqlite
*.sqlite3

# --- Invoices (generated mocks AND real/private invoices) ---
samples/*.pdf
samples/*.png
samples/*.jpg
samples/*.jpeg

# --- Recovered prior-session bytecode (local only) ---
.recover/

# Excel/Word lock + temp files
~$*
.Rhistory

# Autonomous intake (private — invoices + processed ledger)
inbox_attachments/
processed.json
evidence_data/
ingest.log
